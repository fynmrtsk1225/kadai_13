<%= form_with(model: picture, local: true) do |f| %>
  <% if picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(picture.errors.count, "error") %> prohibited this picture from being saved:</h2>

      <ul>
        <% picture.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %>
    <%= image_tag(picture.image.url) if picture.image && picture.image.url %>
    <%= f.file_field :image %>
    <%= f.hidden_field :image_cache %>
  </div>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_field :content %>
  </div>


  <div class="d-flex justify-content-between">
    <%= link_to_add_association 'タグ追加', f, :picture_tags,
        class: 'btn btn-success  ml-1 p-1',
        data: {
          association_insertion_node: '#detail-association-insertion-point',
          association_insertion_method: 'before' }
    %>
  </div>
  <div class="form-group" id='detail-association-insertion-point'>
    <%= f.fields_for :picture_tags do |form| %>
      <%= render 'picture_tag_fields', f: form %>
    <% end %>
  </div>

  <div class="d-flex justify-content-between">
    <%= link_to_add_association '商品追加', f, :products,
        class: 'btn btn-success  ml-1 p-1',
        data: {
          association_insertion_node: '#detail-association-insertion-point',
          association_insertion_method: 'before' }
    %>
  </div>
  <% unless picture.products.ids.blank? %>
  <div class="form-group" id='detail-association-insertion-point'>
    <%= f.fields_for :products do |form| %>
      <%= render 'product_fields', f: form %>
    <% end %>
  </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
