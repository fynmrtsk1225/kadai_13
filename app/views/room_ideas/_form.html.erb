<%= form_with(model: room_idea, local: true) do |form| %>
  <% if room_idea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room_idea.errors.count, "error") %> prohibited this room_idea from being saved:</h2>

      <ul>
        <% room_idea.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
